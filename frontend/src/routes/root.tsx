import { FC } from 'react';

import { Navbar } from '../components/layout';
import '../styles/main.scss';

const Root: FC = () => {
  return (
    <div>
      <Navbar />
    </div>
  );
};

export default Root;
